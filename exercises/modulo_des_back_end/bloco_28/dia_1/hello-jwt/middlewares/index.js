const error = require('./error');
const login = require('./login');
const auth = require('./auth');
const seePrivilege = require('./seePrivilege');
const secret = require('./secret');

module.exports = {
  error,
  login,
  auth,
  seePrivilege,
  secret,
};
